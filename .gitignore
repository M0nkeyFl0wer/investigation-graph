# Data (never commit investigation data to a public repo)
data/
ingest/
briefings/
*.db
*.kuzu/
*.lbug/

# Analysis outputs
analysis-report-*.json

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Environment
.env

# OS
.DS_Store
Thumbs.db

# Eval evidence artifacts
eval/evidence/

# Fed Filing case — INSTRUCTOR COURSE MATERIAL, NOT PUBLIC.
# The whole case (case study, viz, evidence, manifests, private report, inputs,
# breach captures) stays local for the OSINT instructor; nothing Fed Filing ships
# in this public repo. Integrity stays provable locally from the manifest SHA-256
# hashes; the capture is reproducible from the code + source URLs. The reusable,
# de-branded version of the viz/process template lives with the PUBLIC samples.
examples/fedfiling-case/

# Private OSINT toolkit: course tools/techniques + SpiderFoot/etc. skills. NEVER
# committed to this public repo. Self-contained so it can be zipped and shared with
# the course instructor privately.
osint-toolkit/

# Minimization denylist lists private PII tokens — never commit it (gate fail-closes if absent).
scripts/minimization_denylist.txt
